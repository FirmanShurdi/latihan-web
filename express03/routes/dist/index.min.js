"use strict";var express=require("express"),router=express.Router(),db=require("../db");router.get("/",function(e,t){db.query("\n    SELECT products.id, products.name, products.price, categories.name AS category\n    FROM products\n    LEFT JOIN categories ON products.category_id = categories.id\n  ",function(e,r){if(e)throw e;t.render("home",{products:r})})}),module.exports=router;
//# sourceMappingURL=index.min.js.map
